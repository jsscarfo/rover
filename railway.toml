[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && npm install -g @endorhq/rover@latest"

[deploy]
startCommand = "ROVER_BIN=/usr/local/bin/rover pnpm web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
