[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @endorhq/rover build"

[deploy]
startCommand = "pnpm web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
