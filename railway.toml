[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && npm install -g @endorhq/rover@latest"

[deploy]
startCommand = "pnpm web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
